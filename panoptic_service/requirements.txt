fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
numpy==1.26.4
Pillow==10.4.0
scikit-image==0.24.0

# Match whatever /home/wlli/project/PytrochDeepyeast/detectron2 (vendored
# detectron2 0.6) was verified against -- see README.md.
torch==2.4.1
torchvision==0.19.1
opencv-python-headless
scipy

# detectron2 0.6's pure-Python deps (no compiled extension needed: this
# model's ROI/NMS ops resolve to torchvision, and deformable conv is off
# in the yeast config).
fvcore
iopath
omegaconf
yacs
tabulate
cloudpickle
portalocker
termcolor
tqdm
matplotlib
pycocotools
kornia
# pip install 'git+https://github.com/cocodataset/panopticapi.git'
